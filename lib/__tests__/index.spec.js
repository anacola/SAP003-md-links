const mdLinks =require("../lib/__testes__/index.js");

describe("mdLinks", () => {
  it("is a function", () => {
    expect(typeof mdLinks).toBe("function");
  });

  it("retorna", () => {
    expect(typeof mdLinks).toBe("function")
  });
});
